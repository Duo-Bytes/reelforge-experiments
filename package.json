{
  "name": "reelforge-experiments",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@11.0.4",
  "scripts": {
    "dev": "pnpm --filter $FILTER dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^4.1.8"
  }
}
