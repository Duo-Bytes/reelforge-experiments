import type { Metadata } from "next";
import { Geist_Mono } from "next/font/google";
import "./globals.css";

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Exp-22 · GPU Text Rendering (MSDF) for Titles & Lower-Thirds",
  description:
    "Bake an SDF atlas from a runtime-loaded font, upload to a GPUTexture, and render text in WGSL with fwidth-based anti-aliasing that stays crisp at any zoom or rotation.",
};

export default function RootLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  return (
    <html lang="en" className={`${geistMono.variable} h-full antialiased`}>
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
